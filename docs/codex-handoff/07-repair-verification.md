# 图片与本课目录修复记录

日期：2026-09-11。修复基线：`fd169f920ce2812b71e429344a116cc27947aa1b`。

本文件记录修复分支的当前实现。01–06 保留为历史需求与问题记录；其中“当前加载 media-final.js / 右侧目录消失”等描述指修复前版本，不能当成此分支现状。

## 已授权范围与设计

用户已授权修复图片、右侧目录，并要求图片少占空间、服务正文。

- 主图最大宽 520px，场景图 440px，手机按容器缩放，不裁图；可点击原图查看细节。
- 每图三个编号、图下紧凑双语说明与 US 点读；不强制给所有 Scene 配图。
- 四张场景图片直接来自用户的本地风格参考，没有重新生成或按人脸照片重绘。人脸相似度不属于本次已完成验收。
- 已明确未来女性参考女性照片、男性参考男性照片；原始人脸照片未复制入仓库。
- 桌面宽度 >=1100px 为右侧常驻六模块目录，为目录预留独立列；较窄窗口使用顶部工具栏的“本课目录”展开入口，避免目录按钮遮住图片。
- 原有六模块、左侧课程层级、搜索、上下滚动按钮、Adam Stone 音频保持。

## 根因证据

旧 8 个 WebP 中有 6 个实际字节数小于 RIFF 声明长度，4 个 `-v3.webp` 全部异常。例：`hero-v3.webp` 实际 15176 字节，声明 39114 字节。此证据支持文件不完整，但不能单独证明历史每次故障或模糊都由同一个原因引起。

旧页面同时加载 site-v2.js 的媒体注入和 media-final.js 的替换逻辑。后者删除前者节点并使用被否定的扁平 SVG。旧右侧导航在已停止加载的 lesson-enhancements.js 中。

## 当前正式媒体来源

配置位于 `curriculum/chapter-1/unit-1/part-1.json` 的 heroMedia 和 scene.media；由 app.js 一次渲染。site-v2.js 仅保留原有音频增强。入口不再加载 media-final.js，旧脚本和旧资产保留供回滚、未删除。

| 位置 | 源文件 | 正式资产 | 尺寸 |
|---|---|---|---|
| Hero | 晨光中的慵懒少女.png | hero-dc818fed948b.webp | 1672×941 |
| Alarm | 慵懒晨光中的闹钟时刻.png | alarm-2aac0d52ee9f.webp | 1448×1086 |
| Overslept | 闹钟响起的慌乱早晨.png | overslept-f51798f14760.webp | 1448×1086 |
| Mirror | 晨光镜前的疲惫凝视(1).png | mirror-8d2c27fce6fe.webp | 1448×1086 |

转换为无损 WebP，已逐像素比较解码结果与源图 RGB，四张全部一致。总资源约 6 MB；优先保证画质与可靠性，本轮没有有损压缩。文件名包含内容哈希，JS/CSS 入口版本更新。

图片未成功加载时隐藏整个图像区域和编号，只保留短提示、图下表达与点读。不删除后再由 observer 重建，因此失败不会产生反复请求的循环。

## 验证

- `node --check app.js` 与 `node --check site-v2.js`。
- `python tests/validate_media.py`（需要 Pillow）：验证全部正式 WebP 文件头长度、完整解码、尺寸、内容哈希、词汇映射；比较课程内容和原有音频代码/元数据与基线完全一致。
- 本地浏览器：四张图片 naturalWidth 正确；1440px 宽下主图约 518px、场景图约 438px。
- 桌面六项导航均点击到正确 section，焦点正确，滚动后当前项更新。
- 390px、430px 宽度：页面 scrollWidth 等于 clientWidth，无横向溢出；紧凑目录显示同样六项。
- 搜索 pajamas 匹配两个词汇卡，清空恢复全部 41 项；词汇展开/收起有效。
- 图片点读进入播放状态后正常退出；未发现音频错误样式或控制台异常。此项不是对声音身份的重新鉴定，原始音频数据和播放器完全保留。
- 独立测试服务器强制四图 404：四个图像区域高度均为 0、可见图上编号为 0，12 个点读按钮和六项目录仍存在。

## 后续验收边界

本地浏览器通过不等于 GitHub Pages 已发布通过。合并部署后仍需核对线上四个图片请求、尺寸、导航和实际设备音频。镜前图的浮肿/黑眼圈较轻，人物人脸重绘仍需独立样图确认，不能把本次直接接图描述为人脸定制完成。
